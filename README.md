# AIA Website

## Run locally

1. Install dependencies:

```bash
bun install
```

2. Start the development server:

```bash
bun run dev
```

3. Open the local URL shown in the terminal (usually http://localhost:3333).

## Build for production

```bash
bun run build
bun run preview
```